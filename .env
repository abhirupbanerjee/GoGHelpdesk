# --- REQUIRED ---
DOMAINS="helpdesk.gea.abhirup.app"
EMAIL="mailabhirupbanerjee@gmail.com"

# --- OPTIONAL (sensible defaults set by script) ---
# Set to 1 to use Let's Encrypt STAGING first (no rate limits), then switch to 0 for production.
CERTBOT_STAGING=0

# Database settings (override if you want)
DB_NAME="osticketdb"
DB_USER="ostuser"
# If omitted, the script will auto-generate a strong password
DB_PASS="osTickerPass123!"

# PHP and osTicket versions (defaults are safe)
PHP_VERSION="8.3"
OST_VER="v1.18.2"
